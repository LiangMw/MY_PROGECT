<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.integrate.mingweidev">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".MYApplication"
        android:allowBackup="true"
        android:theme="@style/CyanTheme">

        <activity android:name=".mvp.view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <provider
            android:name=".utils.RongFileProvider"
            android:authorities="com.integrate.mingweidev.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="name,authorities,exported,grantUriPermissions">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"
                tools:replace="name,resource" />
        </provider>

        <activity
            android:name=".mvp.base.BaseActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".mvp.base.BaseFragmentActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".mvp.view.activity.BookActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>